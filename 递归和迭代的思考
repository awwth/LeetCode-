之前一直有点混淆，现在整理一下。

递归：函数不断调用自己
迭代：利用变量的原值推出一个新值

递归的优点是比较方便。可以简单清晰的调用自身函数，但是存在的缺点是利用的空间太大，容易堆栈溢出。
迭代的优点是没有额外的开销，空间基本不怎么增加。但是编码复杂，且不容易理解。

二者关系
	
1） 递归中一定有迭代,但是迭代中不一定有递归,大部分可以相互转换。

2） 能用迭代的不用递归,递归调用函数,浪费空间,并且递归太深容易造成堆栈的溢出./*相对*/
